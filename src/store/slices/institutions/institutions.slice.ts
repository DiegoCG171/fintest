import { createSlice } from '@reduxjs/toolkit';



const initialState = {
  
};

export const institutionSlice = createSlice({
  name: 'institution',
  initialState,
  reducers: {
    // ejemploReducer: (state, action) => { }
  }
});
